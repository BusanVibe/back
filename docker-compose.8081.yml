version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app_8081
    ports:
      - "8081:8080"
    volumes:
      - ./jar:/app
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=prod